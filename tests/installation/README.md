
# Baklava installation test

This directory is meant to simulate a consumer of the Baklava library. The project was generated through:

```shell
$ npm create vite@latest
```

You can use this project to do a quick smoke test in case there are any npm, TypeScript, or linter errors:

```shell
$ npm test
```
